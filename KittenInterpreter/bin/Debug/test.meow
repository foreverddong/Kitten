var isPrime = [num]{
	var flag = false;
	for (var i = 2; i < num;i = i + 1)
	{
		if (num % i == 0)
		{
			flag = true;
		};
	};
	if (flag == false)
	{
		println(num);
	};
};

var i = 0;
println(2);
for (i = 3;i <= 100;i = i + 1)
{
	isPrime(i);
};